--- 
import { frontSkills, backSkills, tools, training } from "../../../const/const";
import { getIconComponent } from "../../../icons/IconAll.astro";
---

<section class="section-skill">
  <div class="neon-overlay"></div>
  <div class="m-auto w-[90%] max-w-[1355px] content-wrapper">
    <!-- Header -->
    <div class="relative lg:max-w-[900px] mt-3">
      <h2 class="text-4xl py-4 title-skills" id="skills">Tecnologías</h2>
      <p class="text-lg nunito text-[#e2e8f0] opacity-90">
        A lo largo de mi carrera, he trabajado con diversas tecnologías que me han permitido mejorar mis proyectos y habilidades técnicas.
      </p>
    </div>

    <!-- Grid Sections -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-8 my-10">
      <!-- Reusable Section -->
      {[
        { title: "Frontend", skills: frontSkills, color: "from-[#f4b4cc] to-[#d87ca1]" },
        { title: "Backend", skills: backSkills, color: "from-[#98c1d9] to-[#5a95b3]" },
        { title: "Aprendiendo", skills: training, color: "from-[#b3e0ca] to-[#81c1aa]" },
        { title: "Herramientas", skills: tools, color: "from-[#fbc490] to-[#e09961]" },
      ].map(({ title, skills, color }) => (
        <div class="bg-gradient-to-br {color} rounded-xl shadow-lg hover:shadow-2xl transition-all p-6 text-center group">
          <h3 class="text-2xl font-bold text-white mb-4 uppercase tracking-wider">{title}</h3>
          <div class="grid grid-cols-2 gap-4 lg:grid-cols-3">
            {skills.map(({ icon, title }) => {
              const IconComponent = getIconComponent(icon);
              return (
                <div class="flex flex-col items-center transform group-hover:scale-105 transition-all duration-300">
                  {IconComponent && (
                    <IconComponent class="w-12 h-12 text-white mb-2 shadow-md group-hover:shadow-lg" />
                  )}
                  <p class="text-sm text-[#f9fafb]">{title}</p>
                </div>
              );
            })}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- Styles -->
<style>
.section-skill {
  position: relative;
  width: 100%;
  background: linear-gradient(to bottom, #1e293b, #0f172a);
  padding: 60px 0;
  color: #f9fafb;
}

.neon-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.05) 0%, transparent 80%);
  pointer-events: none;
}

.content-wrapper {
  position: relative;
  z-index: 10;
}

.title-skills {
  font-size: 32px;
  font-weight: 800;
  color: #f8fafc;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.bg-gradient-to-br {
  transition: all 0.3s ease-in-out;
}

.group:hover .group-hover\:scale-105 {
  transform: scale(1.05);
}

.group-hover\:shadow-2xl {
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
}

@media (min-width: 768px) {
  .title-skills {
    font-size: 40px;
  }
}
</style>
